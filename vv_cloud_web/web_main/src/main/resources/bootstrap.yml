# ################################# 应用服务器配置 ##################################
server:
  port: 80
  servlet:
    context-path: /${spring.application.name}  
spring:
  application:
    name: web_base
  jmx:
    default-domain: ${spring.application.name}
